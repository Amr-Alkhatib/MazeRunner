package Maze.Runner.gameobjects;

import com.badlogic.gdx.graphics.Texture;

/**
 * Maze exit; requires key to use.
 */
public class Exit extends StaticGameObject {
    public Exit(float x, float y, Texture texture) {
        super(x, y, texture);
    }
}
