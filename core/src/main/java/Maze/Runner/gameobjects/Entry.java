package Maze.Runner.gameobjects;

import com.badlogic.gdx.graphics.Texture;

/**
 * Player spawn location.
 */
public class Entry extends StaticGameObject {
    public Entry(float x, float y, Texture texture) {
        super(x, y, texture);
    }
}
