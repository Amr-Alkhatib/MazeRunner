package Maze.Runner.gameobjects;

import com.badlogic.gdx.graphics.Texture;

/**
 * Collectable key required to open exit.
 */
public class Key extends StaticGameObject {
    public Key(float x, float y, Texture texture) {
        super(x, y, texture);
    }
}
